apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
# spec:
#   ports:
#     - port: 3000
#       targetPort: 3000
#       name: web
#   selector:
#     app: grafana
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30035
